export const setProgramLevels = (state, val) => {
  state.programLevels = val;
};

export const setPrograms = (state, val) => {
  state.programs = val;
};

export const setIsLadingStudentPrograms = (state, val) => {
  state.isLadingStudentPrograms = val;
};

export const setStudentPrograms = (state, val) => {
  state.studentPrograms = val;
};
