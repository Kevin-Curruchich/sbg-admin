export const getProgramLevels = (state) => {
  return state.programLevels;
};

export const getPrograms = (state) => {
  return state.programs;
};

export const getIsLadingStudentPrograms = (state) => {
  return state.isLadingStudentPrograms;
};

export const getStudentPrograms = (state) => {
  return state.studentPrograms;
};
